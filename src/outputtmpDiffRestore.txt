```diff
--- a/ra/ra.go
+++ b/ra/ra.go
@@ func (ra *RegistrationAuthorityImpl) issueCertificateInner(
 	issueReq := &capb.IssueCertificateRequest{
 		Csr:            csr.Raw,
 		RegistrationID: int64(acctID),
 		OrderID:        int64(oID),
+		// issuerNameID is a newly added proto field. RA does not currently track or
+		// determine issuerNameID, and CA does not require it for correctness.
+		// We populate it with zero to maintain forward‑compatibility.
+		IssuerNameID:   0,
 	}
```

This minimal change ensures the RA constructs the updated `IssueCertificateRequest` correctly according to the new proto schema without altering any business logic.